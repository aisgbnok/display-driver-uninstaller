<Window x:Name="frmSystemRestore" x:Class="Display_Driver_Uninstaller.FrmSystemRestore"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	Title="frmSystemRestore"
	WindowStartupLocation="CenterScreen"
	ShowInTaskbar="False"
	SizeToContent="WidthAndHeight"
	ResizeMode="NoResize"
	Background="{DynamicResource App.Surface.Window}"
	Foreground="{DynamicResource App.Text.Primary}">

    <Window.Resources>
    </Window.Resources>

    <Grid Background="{DynamicResource App.Surface.Window}">
        <Border Background="{DynamicResource App.Surface.Panel}" BorderBrush="{DynamicResource App.Border}" BorderThickness="1" Padding="18,14">
            <Label x:Name="lblSystemRestore"
				   Content="lblSystemRestore"
				   HorizontalContentAlignment="Center"
				   VerticalContentAlignment="Center"
				   FontFamily="Calibri"
				   FontSize="14"
				   Foreground="{DynamicResource App.Text.Primary}" />
        </Border>
    </Grid>
</Window>
