<Window x:Class="Display_Driver_Uninstaller.FrmMain" x:Name="frmMain"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="clr-namespace:Display_Driver_Uninstaller"
	xmlns:conv="clr-namespace:Display_Driver_Uninstaller.Converters"
	mc:Ignorable="d"

	Title="{Binding Settings.AppName, Mode=OneWay}"
	Height="625" Width="690"
	MinHeight="625" MinWidth="690" ResizeMode="CanResize" Icon="/Display%20Driver%20Uninstaller;component/Resources/DDU.ico"
	Visibility="Visible" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BitmapImage x:Key="imgDonateSource" UriSource="/Display%20Driver%20Uninstaller;component/Resources/btn_donate_LG.gif" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="imgPatronSource" UriSource="/Display%20Driver%20Uninstaller;component/Resources/become_a_patron_button.png" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="imgDiscordSource" UriSource="/Display%20Driver%20Uninstaller;component/Resources/discord@2x.png" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="imgOfferSource" UriSource="/Display%20Driver%20Uninstaller;component/Resources/btn_partner.png" CacheOption="OnLoad" po:Freeze="True" />

        <Style x:Key="DonateButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Image Source="{StaticResource imgDonateSource}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PatronButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Image Source="{StaticResource imgPatronSource}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DiscordButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Image Source="{StaticResource imgDiscordSource}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OfferButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Image Source="{StaticResource imgOfferSource}" ToolTip="{Binding ElementName=lblOffer, Path=ToolTip, Mode=OneWay}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <BitmapImage x:Key="logoAMD" UriSource="/Display%20Driver%20Uninstaller;component/Resources/amd_logo.png" DecodePixelWidth="384" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="logoNvidia" UriSource="/Display%20Driver%20Uninstaller;component/Resources/nvidia_logo.png" DecodePixelWidth="384" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="logoIntel" UriSource="/Display%20Driver%20Uninstaller;component/Resources/intel_logo.jpg" DecodePixelWidth="384" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="logoRealtek" UriSource="/Display%20Driver%20Uninstaller;component/Resources/realtek-logo.png" DecodePixelWidth="384" CacheOption="OnLoad" po:Freeze="True" />
        <BitmapImage x:Key="logoSoundBlaster" UriSource="/Display%20Driver%20Uninstaller;component/Resources/creative_logo.png" DecodePixelWidth="384" CacheOption="OnLoad" po:Freeze="True" />
        <conv:BooleanToVisibilityConverter x:Key="BoolToVisibility" TrueValue="Visible" FalseValue="Collapsed" />
        <conv:BooleanToString x:Key="BoolToStr" TrueValue="x64" FalseValue="x86" />
        <conv:EnumToTextConverter x:Key="EnumToText" />

    </Window.Resources>

    <Window.Style>
        <Style TargetType="{x:Type Window}">
            <Setter Property="Background" Value="{DynamicResource App.Surface.Control}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Settings.SelectedGPU, Mode=OneWay}" Value="{x:Static local:GPUVendor.AMD}">
                    <Setter Property="Background" Value="{DynamicResource App.Brand.Amd}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Settings.SelectedGPU, Mode=OneWay}" Value="{x:Static local:GPUVendor.Nvidia}">
                    <Setter Property="Background" Value="{DynamicResource App.Brand.Nvidia}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Settings.SelectedGPU, Mode=OneWay}" Value="{x:Static local:GPUVendor.Intel}">
                    <Setter Property="Background" Value="{DynamicResource App.Brand.Intel}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Settings.SelectedAUDIO, Mode=OneWay}" Value="{x:Static local:AudioVendor.Realtek}">
                    <Setter Property="Background" Value="{DynamicResource App.Brand.Realtek}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Settings.SelectedAUDIO, Mode=OneWay}" Value="{x:Static local:AudioVendor.SoundBlaster}">
                    <Setter Property="Background" Value="{DynamicResource App.Brand.SoundBlaster}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Grid FlowDirection="{Binding Path=Settings.FlowControl}" Background="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200" MaxWidth="350" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <!-- Space ( 30px ) -->
            <ColumnDefinition Width="250" MaxWidth="300" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <!-- Space ( 30px ) -->
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Menu Name="MenuStrip1" FontSize="14" HorizontalAlignment="Stretch" VerticalAlignment="Top" Grid.ColumnSpan="4" Style="{StaticResource SharedMenuStyle}" ItemContainerStyle="{StaticResource SharedMenuItemStyle}">
            <MenuItem x:Name="OptionsMenuItem" Header="OptionsMenuItem"/>

            <MenuItem x:Name="LinksMenuItem" Header="Links">
                <MenuItem x:Name="VisitDDUHomeMenuItem" Header="DDU Website" />
                <MenuItem x:Name="VisitDDUShopMenuItem" Header="DDU Merch Shop" />

                <Separator />

                <MenuItem x:Name="DownloadNvidiaMenuItem" Header="NVIDIA Driver Downloads" />
                <MenuItem x:Name="DownloadAmdMenuItem" Header="AMD Driver Downloads" />
                <MenuItem x:Name="DownloadIntelMenuItem" Header="Intel Driver Downloads" />

                <Separator />

                <MenuItem x:Name="VisitDiscord" Header="Discord Server" />
                <MenuItem x:Name="VisitSVNMenuItem" Header="GitHub Repository" />
            </MenuItem>

            <MenuItem x:Name="ExtendedLogMenuItem" Header="ExtendedLogMenuItem"/>

            <MenuItem x:Name="InfoMenuItem" Header="InfoMenuItem">
                <MenuItem x:Name="ToSMenuItem" Header="ToSMenuItem" />
                <MenuItem x:Name="AboutMenuItem" Header="AboutMenuItem" />
                <MenuItem x:Name="TranslatorsMenuItem" Header="TranslatorsMenuItem" />
                <MenuItem x:Name="PatronMenuItem" Header="PatronMenuItem" />
            </MenuItem>

            <MenuItem x:Name="TestMenuItem" Header="TESTING" Visibility="{Binding IsDebug, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                <MenuItem x:Name="checkXMLMenuItem" Header="Check language files for errors" />
                <MenuItem x:Name="SetupAPIMenuItem" Header="Debug Tools" />
                <MenuItem x:Name="testingMenuItem" Header="Generic test" />
            </MenuItem>
        </Menu>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="10,5,10,5">
            <TextBlock x:Name="lblWinVersion" Text="lblWinVersionText" VerticalAlignment="Center" Foreground="{DynamicResource App.Text.Primary}" />
            <TextBlock FontWeight="Bold" Margin="3,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource App.Text.Primary}">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} ({1})">
                        <Binding Path="Settings.WinVersionText" />
                        <Binding Path="Settings.WinIs64" Converter="{StaticResource BoolToStr}" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="48,0,10,0" HorizontalAlignment="Right">
            <Image Stretch="Uniform" VerticalAlignment="Center" MaxWidth="384" MaxHeight="120" Margin="0, 0, 0,24">
                <Image.Style>
                    <Style TargetType="{x:Type Image}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Settings.SelectedGPU, Mode=OneWay}" Value="{x:Static local:GPUVendor.AMD}">
                                <Setter Property="Source" Value="{StaticResource logoAMD}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Settings.SelectedGPU, Mode=OneWay}" Value="{x:Static local:GPUVendor.Nvidia}">
                                <Setter Property="Source" Value="{StaticResource logoNvidia}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Settings.SelectedGPU, Mode=OneWay}" Value="{x:Static local:GPUVendor.Intel}">
                                <Setter Property="Source" Value="{StaticResource logoIntel}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Settings.SelectedAUDIO, Mode=OneWay}" Value="{x:Static local:AudioVendor.Realtek}">
                                <Setter Property="Source" Value="{StaticResource logoRealtek}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Settings.SelectedAUDIO, Mode=OneWay}" Value="{x:Static local:AudioVendor.SoundBlaster}">
                                <Setter Property="Source" Value="{StaticResource logoSoundBlaster}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
        </Grid>

        <DockPanel Grid.Row="2" x:Name="ButtonsPanel" >
            <DockPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource SharedButtonStyle}">
                    <Setter Property="MinWidth" Value="250" />
                    <Setter Property="MaxWidth" Value="320" />
                    <Setter Property="MinHeight" Value="37" />
                    <Setter Property="MaxHeight" Value="100" />
                    <Setter Property="Margin" Value="10,10,10,0" />
                </Style>
            </DockPanel.Resources>

            <Button DockPanel.Dock="Top" x:Name="btnCleanRestart" HorizontalAlignment="Stretch" >
                <TextBlock Text="btnRestart" Style="{StaticResource SharedButtonTextStyle}" />
            </Button>
            <Button DockPanel.Dock="Top" x:Name="btnClean" HorizontalAlignment="Stretch" >
                <TextBlock Text="btnClean" Style="{StaticResource SharedButtonTextStyle}" />
            </Button>
            <Button DockPanel.Dock="Top" x:Name="btnCleanShutdown" HorizontalAlignment="Stretch" >
                <TextBlock Text="btnCleanShutdown" Style="{StaticResource SharedButtonTextStyle}" />
            </Button>
            <Button DockPanel.Dock="Top" x:Name="btnCleanCaches" HorizontalAlignment="Stretch" >
                <TextBlock Text="btnCleanCaches" Style="{StaticResource SharedButtonTextStyle}" />
            </Button>
        </DockPanel>

        <DockPanel Grid.Row="4" Grid.Column="3" x:Name="RightButtonsPanel" >
            <DockPanel.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource SharedButtonStyle}">
                    <Setter Property="MinWidth" Value="230" />
                    <Setter Property="MaxWidth" Value="230" />
                    <Setter Property="MinHeight" Value="37" />
                    <Setter Property="MaxHeight" Value="100" />
                    <Setter Property="Margin" Value="10,10,10,0" />
                </Style>
            </DockPanel.Resources>
            <Button DockPanel.Dock="Top" x:Name="btnWuRestore" MinHeight="40" MaxHeight="60" Visibility="{Binding Settings.PreventWinUpdate, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="btnWuRestore" Style="{StaticResource SharedButtonTextStyle}" />
            </Button>
        </DockPanel>

        <DockPanel LastChildFill="True" Margin="10" Grid.Row="5" Grid.Column="3">
            <StackPanel DockPanel.Dock="Bottom">
                <Label x:Name="lblLanguage" Content="lblLanguage" FontWeight="Bold" Foreground="{DynamicResource App.Text.Primary}" />
                <ComboBox x:Name="cbLanguage" ItemsSource="{Binding Settings.LanguageOptions, Mode=OneWay}" SelectedItem="{Binding Settings.SelectedLanguage, Mode=TwoWay}" DisplayMemberPath="DisplayText" VerticalAlignment="Top" Height="22" IsReadOnly="True" Style="{StaticResource SharedComboBoxStyle}" ItemContainerStyle="{StaticResource SharedComboBoxItemStyle}" />
            </StackPanel>

            <StackPanel Visibility="{Binding Settings.ShowOffer, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" DockPanel.Dock="Top" VerticalAlignment="Center">
                <Label x:Name="lblOffer" FontWeight="Bold" Content="lblOffer" HorizontalContentAlignment="Center" FontSize="11" Foreground="{DynamicResource App.Text.Primary}" />

                <Button x:Name="imgOffer" Style="{StaticResource OfferButtonStyle}" />
            </StackPanel>
        </DockPanel>

        <Label Content="lblLog" Grid.Row="3" Name="lblLog" VerticalContentAlignment="Center" FontWeight="Bold" Margin="0,2.04,0,2" VerticalAlignment="Bottom" Foreground="{DynamicResource App.Text.Primary}" />

        <ListBox Name="lbLog" Focusable="False" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,3,0,0" Grid.RowSpan="2" Style="{StaticResource SharedListBoxStyle}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource SharedListBoxItemStyle}">
                    <Setter Property="Focusable" Value="False" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <Label Name="lblUpdate" Content="{Binding Settings.UpdateAvailable, Mode=OneWay, Converter={StaticResource EnumToText}}" Grid.Row="6" Grid.ColumnSpan="4" Margin="0,0,10,0" Foreground="{DynamicResource App.Text.Primary}" />

        <Border Grid.Row="7" Grid.ColumnSpan="4" Margin="0,10,0,0" Background="{DynamicResource App.Surface.Status}" BorderBrush="{DynamicResource App.Border}" BorderThickness="1,1,1,0">
            <Grid HorizontalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="96" />
                    <ColumnDefinition Width="99.362"/>
                    <ColumnDefinition Width="33.362"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="lblDonate" Grid.Column="0" Text="lblDonate" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center" Margin="6,5.585,6,5.584" Foreground="{DynamicResource App.Text.Primary}" />

                <Button x:Name="imgDonate" Grid.Column="1" HorizontalAlignment="Left" Width="96" Style="{StaticResource DonateButtonStyle}" />
                <Button x:Name="imgPatron" Grid.Column="2" Width="96" Margin="0,0,3.362,0" HorizontalAlignment="right" Style="{StaticResource PatronButtonStyle}" />
                <Button x:Name="imgDiscord" Grid.Column="3" Width="32" Margin="0,0,3.362,0" HorizontalAlignment="right" MaxHeight="32" Style="{StaticResource DiscordButtonStyle}" />
            </Grid>
        </Border>
        <DockPanel Grid.Column="3" Grid.Row="3" >
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <ComboBox Name="cbSelectedType" HorizontalAlignment="Stretch" VerticalAlignment="Center" DockPanel.Dock="Top" Margin="10,0,10,0" SelectedIndex="0" Style="{StaticResource SharedComboBoxStyle}" ItemContainerStyle="{StaticResource SharedComboBoxItemStyle}" />
                <ComboBox x:Name="cbSelectedGPU" HorizontalAlignment="Stretch" VerticalAlignment="Center" DockPanel.Dock="Top" SelectedIndex="0" Margin="10,0,10,0" Style="{StaticResource SharedComboBoxStyle}" ItemContainerStyle="{StaticResource SharedComboBoxItemStyle}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
